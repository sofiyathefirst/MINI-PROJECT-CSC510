int main()
{
    enum{S0, S1, S2, S3, S4, Sstop} state;

    state = S0;
    while(state != Sstop)
    {
        switch(state)
        {
            case S0;
            
        }
    }
}